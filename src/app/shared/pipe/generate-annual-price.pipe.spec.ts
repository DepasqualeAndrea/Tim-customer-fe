import { GenerateAnnualPricePipe } from './generate-annual-price.pipe';

describe('GenerateAnnualPricePipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateAnnualPricePipe();
    expect(pipe).toBeTruthy();
  });
});
